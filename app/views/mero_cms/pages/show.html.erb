<div id="page">
  <h2><%= @page.title %></h2>
  <%= div_for(@page) do %>
    <%= @page.content %>
  <% end %>
  <nav class="actions">
    <%= link_to "Edit", edit_page_path(@page) %>
    <%= link_to "Destroy", page_path(@page), :method => :delete %>
  </nav>
</div>